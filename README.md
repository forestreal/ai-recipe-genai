# AI Recipe GenAI

This repo contains two versions:

- **v2/** → Current active version (Docker + FastAPI + Gemini integration)
- **v1/** → Legacy version (initial prototype)

Run `docker compose up -d` from inside v2/ to launch the full stack.

